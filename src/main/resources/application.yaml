spring:
  application:
    name: Task-Management-System
  datasource:
    url: jdbc:postgresql://localhost:5432/task_management_system
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
